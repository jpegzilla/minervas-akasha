/* eslint-disable */

import React, {
  useState,
  useEffect,
  createElement,
  cloneElement,
  Children
} from "react";

// turns a component into a draggable box
const Drag = props => {
  const { children } = props;

  return;
};
